# Steam Docker Runner

## .envrc
`cp .envrc.example .envrc`
`vi .envrc`
`direnv allow`

## Install
`./docker/build`

## Run
`./docker/start`

You may want to edit data/configs/\*.cfg after your first run
